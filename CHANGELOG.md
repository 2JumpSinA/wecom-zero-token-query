# 变更记录

本技能按"取数层 / 通道层 / 部署层"三层组织（见 `references/00-架构总览.md`）。
版本号在 `VERSION` 文件里；打包用 `scripts/pack.ps1`（会自动排除 logs/、__pycache__、.git 等）。

## v2.3.0 — 多脚本共用的 webhook key：收拢到一处

由来：真实项目里同一个群的 webhook key 被**硬编码在 3 个播报脚本**里，轮换一次要翻三个文件，
还不能漏 —— 而漏掉的那一个不会报错，只会在某个时刻静默地不再发消息。

**新增能力（部署层）**
- `assets/webhook_key.py`：webhook key 的**唯一读取入口**。脚本侧从"写死一整条带 key 的 URL"
  变成 `WEBHOOK = webhook_url('quanyi')`；轮换 key 只改 `webhooks.json` 一处
  - 读取优先级：环境变量（`WECOM_HOOK_<名字>`）→ `webhooks.json`（可用 `WECOM_HOOKS_FILE` 换位置）→ **当场报错**
  - 为什么读不到要报错而不是返回空值：空 key 会让脚本发一个必然失败的请求，
    真正的原因（配置没读到）被伪装成"发送失败" —— 静默失败的经典形态
- `assets/check_webhook_keys.py`：自检，证明"key 只在一处"。扫描指定目录、
  **点名**还有哪个文件写着明文 key（自动跳过 `.git` / `logs` / `_archive` / `runtime` 等），可直接挂进 CI 或体检脚本
- `references/09` 新增 §4.1：完整做法 + 四条设计要点（优先级 / 纯文件存放 / 兜底脚本也要读同一处 /
  自检要能抓"又写回去"）+ 验收方法（断言新旧地址**逐字相同**，比真发一条更省事也更安全）+ 两个坑

**新增测试**
- `tests/test_webhook_keys.py`：**13 条断言级回归**（读取契约 7 条 + 自检脚本 6 条）。
  重点钉住"读不到必须报错"，以及"自检真的能发现写死的 key，又不会把 `webhooks.json` 自己与占位符误报成命中"
- 全套 **38 条**测试通过（原 25 + 新 13）

**文档**
- `references/05` 补两条 webhook 相关症状（某个群收不到消息 / `errcode=93000 invalid webhook url`）
- `references/09` 迁移清单加一项（**统一 key 文件** —— 漏了它新机器上一句都发不出）、可移植性自查表加一行
- `SKILL.md`：文件清单与自检清单同步；"验证只打印之前先读代码"这条教训也写进了 §4.1

## v2.2.0 — 查询改读采集结果（变快，而且平台挂了也能答）

**新增能力（通道层）**
- **采集与缓存**（新文档 `references/11-采集与缓存.md`）：把「查询＝现场抓取」换成「查询＝读采集结果」。
  命令上加一个 `"snapshot": "<源名>"` 字段即接入，**不写新脚本**；未配的命令行为一字不变，可以一条一条地上
  - 采集器 `collect`：守护进程**内置定时器**（每 `everyMinutes` 分钟），把所有带 `snapshot` 的命令按
    「只打印」模式跑一遍 → 写 `snapshots\<源>.json`（临时文件 + rename 原子替换，读侧永远看到完整版本）
  - 收益：一条 20.4 秒的查询降到 **1–4ms**
  - **采集失败也照常答**（只要有正文、数据时间没超 `maxAgeMinutes`）—— 这比"变快"更重要；
    正文首行会带 `> 数据时间：HH:MM（N 分钟前）`
  - `freshness`：`staleMinutes` 超了在正文里加一行陈旧警告；`maxAgeMinutes` 超了才回退现场抓取
- **采集陈旧告警** `alerts.stale`：每轮采集结束后判定各源数据时间，超线（或从来没成功过）就主动推一条。
  采集器没有"出事的那条会话"，所以推送目标是配置里列出的群 —— **默认只推运维群**，技术故障不往业务群推
- **历史留档** `history`：每轮往 `history\<源>\<日期>.jsonl` 追加一条（append-only，
  被强杀最多丢最后一行），`keepDays` 之后自动清理；`--history` 回看某天每轮采到了什么、有没有空档
- 群内「状态」命令增加**数据源**段：每条源的数据时间与陈旧/失败标记

**新增命令（CLI）**
- `--collect [源]`（手动采一轮：联网取数、不发群）· `--query <命令> [--as-group <群ID>]`
  （离线预演群内渲染：读缓存 → 作用域裁剪 → 数据时间，与线上走同一段代码）
- `--stale-selftest`（告警判定状态机，喂样本）· `--stale-check`（拿真实快照预演"会推什么"，不推群）
- `--history [源] [--date YYYY-MM-DD]`

**新增部署件（可选路径）**
- `assets/collect-hidden.vbs` + `assets/register-collect-task.ps1` + `assets/注册采集任务.cmd`：
  想把采集改成**系统计划任务**时用（默认不用 —— 采集器跑在守护进程里，见 `references/11` §4）

**新增测试**
- `tests/test_cache_layer.py`：**15 条断言级回归**（在临时沙箱里跑真实 `fastlane.mjs`，
  不联网、不需要 SDK、不推任何群）。其中三条专门钉住踩过的坑：
  1. 采集失败**不得**推进「数据时间」（否则陈旧告警永远不触发）
  2. 采集失败后，查询**仍要**用缓存答（否则平台一挂就白改）
  3. 保留期清理**只删**过期文件（日期比较写反会删掉全部留档，而且悄无声息）
- 全套 **25 条**测试通过（原 10 + 新 15）

**文档**
- `references/00` 数据流图补上"采集路径"（那条不经过消息的路径），并记下 v2.2.0 补了什么
- `references/02` 补 `snapshot` / `collect` / `freshness` / `history` / `alerts.stale` 字段
- `references/04` 补内置采集器（为什么不是计划任务）；`references/05` 补缓存类症状的排查条目与新调试命令；
  `references/09` 补目录约定与磁盘占用估算
- `SKILL.md`：新增硬规则「缓存必须配一条"它有多旧"的出口」，自检清单加缓存 / 告警 / 破坏性演练三项
- ⚠️ `docs/标准操作手册.docx` 这一轮**没有**重生成（只更新了 `.md`）

## v2.1.0 — 凭据生命周期（过期不再是"等失败才发现"）

- **失败回执里的人话提示**（`authHints`）：命中登录态/401/Cookie 失效等特征时，回复里多一行「怎么办」，
  而不是把 traceback 甩给同事。配置规则优先、内置规则兜底；`--hint-selftest` 可离线验证命中情况
- `references/07` 增加**登录态探测脚本的最小配方**（只读接口 + token 打码 + 总是退出 0 的三条注意）
- `references/02` 补 `authHints` 字段说明
- 配套的部署侧实践：把探测做成群命令（如「登录态」），谁都能随时问一句

## v2.0.0 — 通用化重构

**结构**
- 新增 `references/00-架构总览.md`：三层职责边界、数据流、通用性从哪来（含"取数层不可能通用"的结论）
- 新增 `references/07-取数路径.md`：五类数据平台的选路决策树与配方
- 新增 `references/08-脚本接入契约与探针.md`：四条契约成文 + `--probe` 用法
- 新增 `references/09-本地化部署.md`：环境探测、变量路径、凭据生命周期、离线安装、升级回滚、迁移清单
- 新增 `references/10-泛化模型与作用域.md`：把"品牌"泛化成任意维度 + 多租户闸门

**新增能力（通道层）**
- 配置**变量展开**：`${HERE}` `${DSH_HOME}` 任意环境变量 → 换机器不用改路径
- **通用作用域** `scope` / `scopeByGroup`（旧 `brands` / `brandScope` 仍兼容）
- **`--probe` 探针**：把任意脚本变成命令配置，并校验契约（退出码 / 正文 / 裸标签 / 耗时→timeoutMs）
- **`--status` + 群内「状态」内建命令**：随时查进程、连接、心跳、当前任务、最近执行
- **连续失败告警**：同一命令连续失败 N 次主动推群，冷却期内不重复，成功即清零（`alerts` 配置）
- **`--alert-selftest`**：不发一条消息就能验证告警状态机
- 配置 **BOM 容忍**（记事本/PowerShell 写出的 JSON 常带 BOM，原先会直接启动失败）
- `--check` 增加**安全护栏提示**：未检出预演开关的命令、以及对所有人开放时的边界提醒

**新增能力（取数层）**
- `examples/adapter-api.py`（正式 API）、`adapter-private-api.py`（私有接口）、`adapter-export-file.py`（导出文件）
  三个可跑样板，均支持 `--demo` 离线跑通链路
- 样板 B 内置真实项目提炼的模式：token 注入优先、失效标志识别、重试退避、参数加解密
- 样板 B 增加**登录态续期三件套**：`--check-auth` / `--set-token` / `refresh_session()` 钩子
- 样板 C 的已知非异常清单支持环境变量覆盖（便于测试与共享）

**新增能力（部署层）**
- `scripts/probe-env.ps1`：Node / Python / 网络（企微长连接、npm、PyPI、GitHub API）/ 计划任务权限 / 磁盘
- `scripts/pack.ps1`：干净打包（排除日志与缓存）
- `tests/test_adapters.py`：10 条断言级回归（契约 5 条 + 逻辑 4 条 + 失败路径 1 条）

**修复**
- 含中文的 `.ps1` 在无 BOM 时被 PowerShell 5.1 按 GBK 误读 → 中文乱码（三个脚本已加 BOM）
- 探针的参数解析丢弃"值本身以 `--` 开头"的参数（如 `--preview-flag --demo`）
- 安全护栏把内建命令误判为"可能推群的脚本"
- 适配器 docstring 里的 Windows 路径触发 `\x` 转义错误

## v1.0.0 — 首版

- 企微智能机器人长连接 → 本地脚本 → 回群的零 token 通道
- 命令表 / 三级白名单 / 群作用域裁剪 / 流式占位回复 / 剥 HTML 标签
- 计划任务 + 看门狗（进程 + 心跳 + pid 三条件）+ 静默启动（wscript，不闪窗）
- 标准操作手册（Markdown + Word）
