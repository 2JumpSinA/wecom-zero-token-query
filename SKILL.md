---
name: wecom-zero-token-query
display_name: 企微零 Token 数据查询直调通道
version: 2.2.0
agent_created: true
description: >-
  把「固定几类数据查询」做成企业微信里的零 token 直调入口：群里 @机器人 发命令 → 本机脚本直接取数 → 结果回到群里，
  全程不经过 AI agent、不消耗 token；并把它做成可长期常驻、可安全开放给同事的服务。
  适用于用户说「企微群里 @ 机器人查数据」「不想每次都花 token 让 agent 跑脚本」「想让同事也能查但不想给机器操作权限」
  「把 bot 只当查询按钮用」「给我的播报脚本加一个群里手动查一次的口子」等场景。
  不适用于自由提问、多步推理、写操作 —— 那些仍应走 agent 通道。
---

# 企微「零 Token 数据查询」直调通道

## 0. 这套东西是什么

群里 @一个机器人发固定命令（`余额` / `门店` / `订单`），**本机脚本直接跑并把结果贴回那条消息**，
全程不经过 AI agent、不调用任何模型 —— **token 增量为 0**。

它与 agent 通道**并存分流**，不是替换：

| | @AI 助手（agent 通道） | @数据查询（本通道） |
|---|---|---|
| 谁处理 | DSH + 模型 | 本地脚本直出 |
| token | 消耗（每条消息都要重放上下文） | **0** |
| 能力 | 自由提问、多步任务 | 固定几条命令 |
| 能否开放给同事 | 等于交出机器操作权，谨慎 | **可以**，能力上限就是你放进去的只读脚本 |

**先跟用户确认这句话**：「固定、重复、可枚举的只读查询走这条路；自由提问仍走 agent。」
如果用户想要的是"用自然语言随便问"，这个 skill 不适用，别硬套。

## 0.5 三层架构（三十秒版）

这套方案拆成三层，**改一层不会牵动另外两层**（详见 `references/00-架构总览.md`）：

| 层 | 职责 | 通用性 | 文档 |
|---|---|---|---|
| **取数层** | 从数据平台把数据拿下来变成文本 | ★☆☆☆☆ 平台各异，**不可能完全通用** | `07` 选路决策树 · `08` 脚本契约 |
| **通道层** | 收企微消息 → 匹配命令 → 跑脚本 → 回群 | ★★★★★ 与数据源零耦合 | `01` `02` `10` `11` `05` |
| **部署层** | 环境、凭据、常驻、离线、升级 | ★★★★☆ | `04` `09` |

**三层唯一的耦合面是「脚本契约」**（只读可预演 / 正文可提取 / 退出码有语义 / 无交互有超时）。
所以"支持什么平台"这个问题被消解了：**平台由你的脚本决定，通道只认契约。**
新增一个查询 = 写一个满足契约的脚本 + 填一段配置 + 跑一次探针。

## 1. 前置条件（缺一不可，先逐条核对）

| 条件 | 怎么确认 | 缺了怎么办 |
|---|---|---|
| 企微**管理员**权限 | 能进 work.weixin.qq.com 后台 | 让管理员代建机器人，把 botId + secret 给你 |
| 一台**长期开机且已登录**的 Windows | —— | 说明清楚：进程不在，机器人就没反应 |
| Node.js ≥ 18 | `node -v` | 装 Node |
| 每个查询有一个**只打印不推送**的脚本 | 见 §3 | 让 agent 按 §3 的接口约定生成 |

## 2. 五步落地（每步都有验收，不要跳）

### §2.1 企微后台建一个「智能机器人」

1. 管理后台 → **智能机器人** → 新建（名字如 `数据查询`）
2. **接收方式必须选「长连接 / WebSocket 长连接」** ← 这一条决定了后面不需要公网 IP、不需要回调 URL、不需要备案
3. 记下 **botId**（`aib…`，非秘密）和 **secret**（密码，绝不外发）
4. 把机器人**加入目标群**（不加进群，@ 它没反应）

**验收**：botId + secret 到手，机器人已在群里。

> 常见错误：进到"自建应用"里配回调 URL。那需要公网入口和 5 秒被动回复限制，本方案不走那条路。
> 如果后台只让你填回调地址，说明进错页面了。

### §2.2 落地程序目录

把本 skill 的 `assets/` 拷到目标目录（例如 `D:\wecom-fastlane\`），再：

```cmd
cd /d D:\wecom-fastlane
npm i @wecom/aibot-node-sdk          :: 没有独立 node_modules 时可跳过，程序会去 DSH 的模块回退目录找
copy commands.example.json commands.json
```

**验收**：`run-fastlane.cmd --check` 输出 SDK OK / botId / secret / 白名单 / 命令数。
（此时 secret 还没放，报"缺失"是正常的。）

### §2.3 准备"只打印不推送"的脚本（三条接口约定）

> **有现成脚本时，先跑探针**：`run-fastlane.cmd --probe <脚本路径> --run [--preview-flag --dry]`
> 它会自动猜解释器、生成可粘贴的配置条目，并**当场校验契约**（退出码 / 正文可提取 / 无裸标签 / 实测耗时→建议 timeoutMs）。
> 只有探针报 FAIL 时，才需要按下面的约定去改脚本。完整说明见 `references/08`。
>
> **没有现成脚本时，从 `examples/` 复制一个样板改**（三个：正式 API / 私有接口 / 导出文件），
> 它们都支持 `--demo` 离线先跑通链路，再填凭据。

**这是整个方案能不能用的关键。** 脚本必须满足：

1. **只打印、不推送** —— 提供一个开关（`--preview` / `--dry` / 不带 `--send`），带上它时只输出、绝不发群、不写"今天已推"这类状态
2. **正文夹在两条 `====`（≥10 个等号）之间** —— 通道按这个规则抓取正文
3. **用企微 markdown**（`###` 标题 / `>` 行 / `**加粗**`），**不要用 `<font color="...">`** —— 那是群机器人 webhook 才支持的颜色写法，**智能机器人不认**，会原样显示成裸标签

让 agent 生成脚本时，直接把下面这段发给它：

```
请写一个 Python 脚本查询【要查的东西】，必须满足三条约定：
1. 只打印结果，绝不发送消息、绝不上传数据；正文夹在两条 40 个 = 的分隔线之间
2. 正文用企微 markdown：### 标题、> 行、**加粗**；不要用 <font color="..."> 这类 HTML 标签
3. 成功退出码 0，失败打印错误并返回非 0；另加 --preview 参数，带上它只打印、不做任何写操作
再告诉我：用哪个 Python 解释器、完整命令怎么写
```

**验收**：`run-fastlane.cmd --selftest 你的命令` 有正文、`exit=0`，且**没有** `正文里仍有疑似标签` 的告警。

> 如果脚本自己也往群里推一条，群里会同一份数据出现两次 —— 这必须在脚本侧解决，通道不替它兜底。

### §2.4 填命令表与白名单

改 `commands.json`：

- `botId`：§2.1 拿到的
- `secret.txt`：一行 secret（**UTF-8 无 BOM**，用记事本另存容易带 BOM 导致校验失败）
- `allowedUsers`：允许使用者的 userid；**留空数组＝谁都不响应**（安全默认）
- `allowedChats` + `groups`：允许的群 + 群名（日志和帮助文案里会显示群名）
- `commands[]`：每条 = 一个查询，字段见 `references/02-命令表与分群.md`
- **路径不要写死**：支持 `${HERE}`（通道目录）、`${DSH_HOME}`、任意环境变量（如 `${PYTHON_EXE}`），换机器不用改配置（见 `references/09`）
- **需要按群隔离时**用 `scope` / `scopeByGroup`（品牌只是一个维度，渠道/区域/账号同理，见 `references/10`）

**userid / 群 ID 怎么拿**：先随便填个占位，让使用者去群里 @ 一次，然后看 `logs\fastlane.log`：

```
收到消息 chattype=group chat=wrTLE…(品牌A数据播报) user=woTLE… text="订单"
                              ↑ 群 ID                    ↑ userid
```

**验收**：`run-fastlane.cmd --routes` 打出的分群矩阵符合预期 ——
每个群里通用词（`门店`/`订单`）只指向本群品牌的命令，越界命令显示"回帮助"。

### §2.5 常驻（必须走 wscript，否则每分钟闪窗）

```cmd
schtasks /create /tn WecomFastlane /tr "wscript.exe //nologo <目录>\watchdog-hidden.vbs" /sc minute /mo 1 /f /rl LIMITED
schtasks /run /tn WecomFastlane
```

**验收**：等 15 秒后 `logs\heartbeat.txt` 时间在 4 分钟内、内容形如 `… pid=NNN authenticated`；
`logs\watchdog.log` 健康时**什么都不写**（安静就是好事）；屏幕上**不再有黑窗闪烁**。

## 3. 硬规则（每条都是踩坑换来的，别省）

| 规则 | 为什么 |
|---|---|
| **另建一个机器人，不要复用现有 agent 机器人** | 同一个机器人挂两条长连接：@ 一次两边都收到 → 群里双回复，且 agent 照样起一轮、token 照花 |
| **计划任务的动作必须是 wscript.exe，不能是 powershell.exe** | powershell 是控制台程序，`-WindowStyle Hidden` 挡不住创建瞬间的黑窗闪烁 |
| **看门狗匹配进程必须锚定结尾**：`fastlane\.mjs"?\s*$` | 否则 `--selftest`、甚至 agent 运行器自身的命令行都会被匹配到，杀错等于把运行器打掉 |
| **心跳里必须写 pid** | 否则"上一个实例留下的心跳 + 任意活着的进程"会被判成健康，该重启时不重启 |
| **健康要三件事同时成立** | 进程在 + 心跳新鲜（4 分钟内）+ 心跳里的 pid 等于该进程 |
| **剥掉 `<font>` 标签再回群** | 智能机器人的 markdown 不支持颜色标签，不剥就是裸标签 |
| **命令表写死、只放只读脚本** | 能力上限 = 你放进去的脚本；没有任意命令执行能力，才敢开放给同事 |
| **非白名单静默忽略** | 不给任何回应，不暴露机器人存在 |
| **缓存必须配一条"它有多旧"的出口** | 读缓存会把"数据旧了"变成新的静默失败来源 —— 所以数据时间、陈旧警告、陈旧告警、体检里的年龄，一个都不能少（见 `11`） |

## 4. 排查（前 6 条覆盖绝大多数问题）

| 症状 | 先看什么 | 处理 |
|---|---|---|
| 群里 @ 了没反应 | `logs\fastlane.log` 有没有"收到消息" | 没有 → 机器人没进群 / 程序没在跑（看 `logs\fastlane.pid`、`heartbeat.txt`）；有 → userid 或群 ID 不在白名单 |
| 有"收到消息"但没"开始执行" | 同上 | 触发词没命中，用 `--routes` 核对写法 |
| 回复很久才出来 | 脚本耗时 | 正常（一条 20 秒的查询改读采集结果后会降到毫秒级，见 `11`）；占位消息会先到 |
| 正文带「⚠️ 数据已 N 分钟未更新」 | `logs\collect.log` | 采集没采到新数据；`--collect <源>` 手动采一次，看它报什么错 |
| 正文里出现 `<font color="comment">` | 脚本用了 HTML 标签 | 让脚本改用 `**加粗**`；通道也会自动剥 |
| 屏幕每分钟闪一下 | 计划任务动作 | 改走 `watchdog-hidden.vbs`（wscript） |
| 说 `secret 缺失` / 认证失败 | secret 文件 | UTF-8 无 BOM、只有一行、无多余空格 |

完整版（17 条 + 证据采集方法）见 `references/05-排查手册.md`。

## 5. 文件清单

```
wecom-zero-token-query/
├── SKILL.md                      本文件：给 agent 的规程
├── assets/                       可直接复制的运行时代码
│   ├── fastlane.mjs              守护进程（命令表 → 脚本 → 剥标签 → 作用域裁剪 → 流式回群）
│   │                             自带 --check / --routes / --selftest / --list / --probe / --as-group
│   │                             以及采集与缓存：--collect / --query / --stale-selftest / --stale-check / --history
│   ├── commands.example.json     配置模板（脱敏，含变量展开与 collect/freshness/history/alerts.stale 示例）
│   ├── run-fastlane.cmd          启动器
│   ├── start-hidden.vbs          静默启动
│   ├── fastlane-watchdog.ps1     看门狗（短命检查：进程 + 心跳 + pid 三条件）
│   ├── watchdog-hidden.vbs       计划任务入口（wscript，不闪窗）
│   ├── collect-hidden.vbs        采集器计划任务入口（**可选**：默认采集器跑在守护进程里）
│   ├── register-collect-task.ps1 + 注册采集任务.cmd   想把采集改成系统计划任务时的入口（需管理员，同上）
│   └── _preview_check.py         回归测试：证明"preview 绝不偷推"
├── references/                   12 份文档，索引见 §6
├── examples/                     三个可跑样板适配器（A 正式 API / B 私有接口 / C 导出文件）+ README
├── tests/test_adapters.py        10 条断言级回归（契约 5 + 逻辑 4 + 失败路径 1）
├── tests/test_cache_layer.py     15 条断言级回归（采集/缓存/陈旧告警/历史留档；沙箱跑真程序，不联网不发群）
├── scripts/                      install.ps1 一键安装 · probe-env.ps1 环境探测 · pack.ps1 干净打包
├── VERSION / CHANGELOG.md        版本号与变更记录（打包脚本读 VERSION）
└── docs/标准操作手册.md/.docx      给「没有 Python 基础的人」看的逐步手册（可直接转发）
```

## 6. 参考文档索引

| 文件 | 什么时候读 |
|---|---|
| `references/00-架构总览.md` | **先读这个**：三层怎么切、通用性从哪来、为什么取数层不可能通用 |
| `references/01-企微侧准备.md` | 建机器人、加群、拿 userid/群 ID 的细节与坑 |
| `references/02-命令表与分群.md` | 每个配置字段的含义；多群/多品牌互不串味 |
| `references/03-脚本接口约定.md` | 三条约定的由来 + Python `--preview` 参考实现 |
| `references/04-常驻与防闪窗.md` | 计划任务、看门狗三条件、为什么必须 wscript |
| `references/05-排查手册.md` | 完整排查表 + 怎么采集证据求助 |
| `references/06-安全与边界.md` | 开放给同事前必须想清楚的事 |
| `references/07-取数路径.md` | **遇到一个新数据平台**：五类平台的选路决策树与配方 |
| `references/08-脚本接入契约与探针.md` | **要接入一个脚本**：四条契约 + `--probe` 探针用法 |
| `references/09-本地化部署.md` | **换台机器部署**：环境探测、变量路径、凭据、离线、升级回滚 |
| `references/10-泛化模型与作用域.md` | 把"品牌"泛化成任意维度（渠道/区域/账号）；多租户闸门 |
| `references/11-采集与缓存.md` | **查询慢 / 想让平台挂了也能答**：查询改读采集结果（快照 + 数据时间 + 陈旧告警 + 历史留档），每条命令只加一个 `snapshot` 字段 |
| `docs/标准操作手册.md` | 给最终使用者（非技术人员）的逐步手册，可直接转发 |

## 7. 交付前自检清单

- [ ] `--check` 全绿（SDK / botId / secret / 访问策略 / 群绑定 / 命令数）
- [ ] `--probe <脚本>` 通过（契约 2/3 + 无裸标签），并按建议设置 `timeoutMs`
- [ ] `--routes` 分群矩阵正确，越界命令被拒
- [ ] `--selftest` 全部 `exit=0`、正文无裸标签
- [ ] 群里实测一条命令，结果正确且**只有一条回复**（没有双推）
- [ ] 心跳新鲜、`watchdog.log` 安静、屏幕无闪窗
- [ ] 配置里**没有**写死本机绝对路径（用 `${HERE}` / 环境变量）
- [ ] `--status` 输出正常（进程 / 连接 / 心跳 / 最近执行），群里发「状态」也能拿到同一份
- [ ] 连续失败告警已按需开启（`alerts`），并用 `--alert-selftest` 验过状态机
- [ ] 失败人话提示已按需配置（`authHints`），并用 `--hint-selftest` 验过命中
- [ ] 慢查询已改读采集结果（命令上 `"snapshot": "<源名>"` + 顶层 `collect`），并用 `--query <命令>`
      验过「读缓存」与正文首行的「数据时间」
- [ ] 陈旧告警已按需开启（`alerts.stale`，**只推运维群**），并用 `--stale-check` / `--stale-selftest` 验过
- [ ] 破坏性演练做过一次：把某条命令的 `exe` 指向不存在的路径 + 把快照数据时间伪造成 2 小时前
      → 期望「采集如实报失败 + 陈旧告警到群 + 查询仍毫秒返回旧数据并带陈旧提示」
- [ ] 体检里有采集与缓存项（每条源的「数据时间 + 年龄 + 今日留档 N/M 轮成功」）
- [ ] `python -m unittest discover -s tests` 全绿（改了样板或缓存层就跑）
- [ ] 明确告知用户：能力上限 = 放进命令表的只读脚本；secret 不要外发
